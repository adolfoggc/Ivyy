<%= form_with(model: user_task, local: true) do |form| %>
  <% if user_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_task.errors.count, "error") %> prohibited this user_task from being saved:</h2>

      <ul>
        <% user_task.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="field">
    <%= form.label :first_due %>
    <%= form.date_select :first_due %>
  </div>

  <div class="field">
    <%= form.label :repetition %>
    <%= form.number_field :repetition %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
